[phases.setup]
nixPkgs = ["nodejs_22", "python312"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd /app/tv_bridge && npm install && cd /app",
]

[start]
cmd = "python /app/run.py"
